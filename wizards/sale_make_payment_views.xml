<odoo>
    <data>
        <record id="view_sale_make_payment_form" model="ir.ui.view">
            <field name="name">sale.make.payment.form</field>
            <field name="model">sale.make.payment</field>
            <field name="arch" type="xml">
                <form string="Register Payment">
                    <group>
                        <field name="journal_id"/>
                        <field name="amount"/>
                        <field name="payment_date"/>
                        <field name="currency_id" invisible="1"/>
                    </group>
                    <footer>
                        <button name="create_payment" string="Create Payment" type="object" class="btn-primary"/>
                        <button string="Cancel" class="btn-secondary" special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>
    </data>
</odoo>
